apiVersion: v1
kind: Service
metadata:
  name: {{ include "deepwiki.fullname" . }}
  labels:
    {{- include "deepwiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deepwiki.selectorLabels" . | nindent 4 }}
